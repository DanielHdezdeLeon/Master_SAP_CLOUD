projection;
strict ( 2 );
use draft;

define behavior for Z_C_CUSTOMER_TRAVEL_DHL alias CustomerTravel
{
  use create;
  use update;
  use delete;

  use action acceptCustomer;
  use action rejectCustomer;
  use action validateCustomerFormat;
  use action acceptClients;

  use action Resume;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
}