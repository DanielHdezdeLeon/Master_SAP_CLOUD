interface;
use draft;

define behavior for Z_I_CUSTOMER_TRAVEL_DHL alias Z_R_CUSTOMER_TRAVEL_DHL
{
  use create;
  use update;
  use delete;

  use action acceptCustomer;
  use action rejectCustomer;
  use action validateCustomerFormat;

  use action Resume;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
}